#include <bits/stdc++.h>
using namespace std;

int re(int n, string str){
    
}

int main()
{
    int n, m;
    cin >> n >> m;
    string s;
    string t;
    cin >> s >> t;
    int q;
    cin >> q;
    vector<string> w(q);
    for (int i = 0; i < q; i++)
    {
        cin >> w[i];
        string split = split()
        // 詳しくは、test.java を参照
    }
    

    
}